import { default as HomeView } from './Home';
import { default as SettingsView } from './Settings';
import { default as NotFoundView } from './NotFound';

const Views = {
    Home: HomeView,
    Settings: SettingsView,
    NotFound: NotFoundView,
};

export { default as HomeView } from './Home';
export { default as SettingsView } from './Settings';
export { default as NotFoundView } from './NotFound';

export default Views;
